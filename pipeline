pipeline
{
agent any
stages
{
stage ('pull code from scm')
{
steps 
{
git branch: 'main', credentialsId: 'github', url: 'https://github.com/anjaneyulujella/may.git'
}
}
stage ('build docker file')
{
steps
{
sh 'docker build -t uma .'
}
}
}
}
